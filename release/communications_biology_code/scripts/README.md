# Release scripts

Figure-generation entry points are retained under `figure_scripts/`. The release candidate contains only descriptive transformations of derived inputs; it is not a standalone replacement for the full statistical pipeline.
