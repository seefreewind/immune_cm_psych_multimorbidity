# Release configuration

Configure repository metadata and relative project paths here when staging the public repository. Do not add raw GWAS paths, LD reference paths, credentials or absolute personal paths.
